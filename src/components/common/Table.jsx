export default function DataTable({ columns, data, empty = 'Không có dữ liệu' }) { return <div className="table-wrap"><table><thead><tr>{columns.map(c => <th key={c.key}>{c.title}</th>)}</tr></thead><tbody>{data?.length ? data.map((row, idx) => <tr key={row.id || row.maDonHang || row.maMonAn || row.maBan || idx}>{columns.map(c => <td key={c.key}>{c.render ? c.render(row, idx) : row[c.key]}</td>)}</tr>) : <tr><td colSpan={columns.length} className="empty">{empty}</td></tr>}</tbody></table></div>; }
